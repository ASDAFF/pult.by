$(document).ready(function(){    $(document).on('mouseover', '.selectbox', function(){        $(this).find('.dropdown').show();    });    $(document).on('mouseover', '.dropdown', function(){        $(this).find('li').removeClass('selected');    });    $(document).on('mouseleave', '.selectbox', function(){        $(this).find('.dropdown').hide();        var val = $(this).find('.text').text();        $(this).find('.dropdown li').each(function(){            if($(this).find('a').text() == val){                $(this).addClass('selected');            }        });    });    $(document).on('click', '.sort-by a, .limiter a, a.sortbutton, .navigation-pages a', function(){        if(Modernizr.history){            sectionFilter(false, $(this).attr('href'));            return false;        }    });    $(document).on('click', '.view-mode .grid', function(){        var viewId = $(this).data('view');        if($(this).parent().hasClass('selected')){            return false;        }        var view_mode = $('#page').find('.view-mode');        view_mode.find('div:not(.selected)').addClass('selected sel');        view_mode.find('div:not(.sel)').removeClass('selected');        view_mode.find('div').removeClass('sel');        var newproducts_block = $(this).parents('.category-products').find('.newproducts-block');        if(viewId == 'view-mode-1'){            newproducts_block.removeClass('catalog_view');            $.cookie('view', null);        } else{            newproducts_block.addClass('catalog_view');            $.cookie('view', viewId);        }    });});